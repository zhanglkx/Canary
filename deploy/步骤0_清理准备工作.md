# 步骤 0：清理准备工作

时间：2025-11-16  
执行人：我  
目标：删除之前的部署尝试文件，重置为干净的初始状态

## 命令/操作

### 已删除的文件：
- ✅ `deploy-to-aliyun.sh` - 旧的部署脚本
- ✅ `server-maintain.sh` - 旧的维护脚本
- ✅ `env.production` - 旧的生产环境配置
- ✅ `nginx.prod.conf` - 旧的 Nginx 配置
- ✅ `canary-deployment.tar.gz` - 旧的部署包
- ✅ `paln.md` - 临时计划文件

### 标准目录结构：
- ✅ `deploy/` 目录已创建，用于存放所有部署记录

## 预期结果

项目根目录恢复到干净状态，只保留：
- 源代码目录：`apps/`、`libs/`
- 配置文件：`docker-compose.yml`、`docker-compose.prod.yml`、`package.json` 等
- 文档目录：`docs/`
- 部署记录目录：`deploy/`（新建）

## 实际结果

所有旧的部署文件已成功删除，项目已重置为初始状态。

## 碰到的问题

无

## 解决方式

无

## 下一步展望

检查服务器连接和系统状态，了解服务器的操作系统、资源情况和已安装的软件。
